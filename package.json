{
  "name": "azure-servicebus-explorer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "paths": {
    "backend": "backend",
    "frontend": "frontend/node-azure-servicebus-explorer-fe"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run backend & npm run frontend",
    "setup": "cd $npm_package_paths_backend && npm i && cd ../$npm_package_paths_frontend && npm i",
    "backend": "cd $npm_package_paths_backend && npm start",
    "frontend": "cd $npm_package_paths_frontend && npm start"
  },
  "author": "",
  "license": "ISC"
}
