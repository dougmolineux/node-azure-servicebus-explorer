{
  "name": "azure-servicebus-explorer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "paths": {
    "backend": "backend",
    "frontend": "frontend"
  },
  "scripts": {
    "install-backend": "cd $npm_package_paths_backend && npm ci",
    "install-frontend": "cd $npm_package_paths_frontend && npm ci",
    "install-app": "npm run install-backend && npm run install-frontend",
    "update-backend": "cd $npm_package_paths_backend && npm update",
    "update-frontend": "cd $npm_package_paths_frontend && npm update",
    "update-app": "npm run update-backend && npm run update-frontend",
    "test-backend": "cd $npm_package_paths_backend && npm test",
    "test-frontend": "cd $npm_package_paths_frontend && npm test",
    "test": "npm run test-backend & npm run test-frontend",
    "start-backend": "cd $npm_package_paths_backend && npm start",
    "start-frontend": "cd $npm_package_paths_frontend && npm start",
    "start": "npm run start-backend & npm run start-frontend"
  },
  "author": "",
  "license": "ISC",
  "prettier": {
    "singleQuote": true
  }
}
